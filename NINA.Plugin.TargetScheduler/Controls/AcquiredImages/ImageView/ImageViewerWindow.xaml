<Window
    x:Class="NINA.Plugin.TargetScheduler.Controls.AcquiredImages.ImageView.ImageViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ninactrl="clr-namespace:NINA.CustomControlLibrary;assembly=NINA.CustomControlLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}"
    Foreground="{StaticResource PrimaryBrush}"
    Width="960"
    Height="720"
    MinWidth="400"
    MinHeight="300"
    ShowInTaskbar="False"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary Source="../../Resources/Resources.xaml" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border
            Grid.Row="0"
            Background="{StaticResource SecondaryBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button
                    x:Name="PART_FitButton"
                    Width="28"
                    Height="28"
                    Margin="4,0,2,0"
                    Click="FitButton_Click"
                    Style="{StaticResource BackgroundButton}"
                    ToolTip="Fit to window">
                    <Grid>
                        <Path
                            Margin="4"
                            Data="{StaticResource Stretch}"
                            Fill="{StaticResource ButtonForegroundBrush}"
                            Stretch="Uniform"
                            UseLayoutRounding="True" />
                    </Grid>
                </Button>
                <Button
                    Width="28"
                    Height="28"
                    Margin="2,0"
                    Click="ZoomInButton_Click"
                    Style="{StaticResource BackgroundButton}"
                    ToolTip="Zoom in">
                    <Grid>
                        <Path
                            Margin="4"
                            Data="{StaticResource ZoomIn}"
                            Fill="{StaticResource ButtonForegroundBrush}"
                            Stretch="Uniform"
                            UseLayoutRounding="True" />
                    </Grid>
                </Button>
                <Button
                    Width="28"
                    Height="28"
                    Margin="2,0"
                    Click="ZoomOutButton_Click"
                    Style="{StaticResource BackgroundButton}"
                    ToolTip="Zoom out">
                    <Grid>
                        <Path
                            Margin="4"
                            Data="{StaticResource ZoomOut}"
                            Fill="{StaticResource ButtonForegroundBrush}"
                            Stretch="Uniform"
                            UseLayoutRounding="True" />
                    </Grid>
                </Button>
                <Button
                    Width="36"
                    Height="28"
                    Margin="2,0"
                    Click="Zoom1to1Button_Click"
                    Style="{StaticResource BackgroundButton}"
                    ToolTip="Actual size (1:1)">
                    <TextBlock
                        Foreground="{StaticResource ButtonForegroundBrush}"
                        FontSize="11"
                        Text="1:1" />
                </Button>
                <TextBlock
                    x:Name="PART_ZoomLabel"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Text="100%" />
            </StackPanel>
        </Border>

        <!-- Image area -->
        <Grid Grid.Row="1">
            <ScrollViewer
                x:Name="PART_ScrollViewer"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto"
                PanningMode="Both"
                PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                MouseLeftButtonDown="ScrollViewer_MouseLeftButtonDown"
                MouseLeftButtonUp="ScrollViewer_MouseLeftButtonUp"
                MouseMove="ScrollViewer_MouseMove">
                <Image
                    x:Name="PART_Image"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Stretch="None"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    Source="{Binding DisplayImage}">
                    <Image.LayoutTransform>
                        <ScaleTransform x:Name="PART_ImageScale" ScaleX="1" ScaleY="1" />
                    </Image.LayoutTransform>
                </Image>
            </ScrollViewer>

            <!-- Loading overlay -->
            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}">
                <ninactrl:LoadingControl
                    MaxWidth="80"
                    MaxHeight="80"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    LoadingImageBrush="{StaticResource PrimaryBrush}" />
            </Grid>
        </Grid>

        <!-- Status bar -->
        <Border
            Grid.Row="2"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,1,0,0"
            Padding="6,3">
            <TextBlock
                FontSize="11"
                Text="{Binding StatusText}" />
        </Border>
    </Grid>
</Window>
